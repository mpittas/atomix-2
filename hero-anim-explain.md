Let's create a badge component that will have two different styles for the colors.

<BadgeHeadingPill color="">

By default the color prop will be "blue" but we'll have option for red as well.

What will change for "color" is background and text color.
Background is solid color and text is gradient text that starts from left to right.

The default badge will be fully rounded
